import { Button } from 'react-bootstrap';
//use props for buttons**

const MainButton = () => (
  <div>
  <Button variant="outline-primary">Primary</Button>{' '}
  </div>
)

export default MainButton;